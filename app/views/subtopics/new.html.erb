<%= form_for @subtopic do |f| %>
	<%= f.label :name %>
  	<%= f.text_field :name %>
  	<%= f.label :text %>
  	<%= f.text_area :text %>
  	<%= f.hidden_field :topic_id, value: params[:topic_id] %>
  	<%= f.submit "Create" %>
<% end %>