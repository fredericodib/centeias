<%= form_for @topic do |f| %>
	<div class="form-field">
		<%= f.label :name %>
  		<%= f.text_field :name, class: "form-control" %>
	</div>
	<br>
	<div class="form-field">
		<%= f.submit "Create", class: "btn btn-primary" %>
	</div>
<% end %>